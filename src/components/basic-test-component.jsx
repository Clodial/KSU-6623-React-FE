import React from 'react';

export const BasicTestComponent = () => {
        return (
            <div>
                <p>basic test component</p>
            </div>
        );
};

export const simpleFunction = (num) => {
    return num;
}